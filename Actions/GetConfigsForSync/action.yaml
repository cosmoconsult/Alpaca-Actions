name: Get Configs For Sync
description: This action finds all secret names from AL-Go settings and Alpaca backend, and encodes specified variables to prepare for synchronization
author: COSMO CONSULT
inputs:
  shell:
    description: Shell in which you want to run the action (powershell or pwsh)
    required: false
    default: powershell
  token:
    description: The GitHub token running the action
    required: false
    default: ${{ github.token }}
  mode:
    description: Mode for the action - 'GetAndUpdate' searches AL-Go settings files and backend, 'Update' only queries backend
    required: false
    default: GetAndUpdate
  includeSecrets:
    description: Comma-separated list of secret names to include
    required: false
    default: ''
  workflowVariablesJson:
    description: Workflow variables as JSON string (from toJson(vars))
    required: false
    default: '{}'
  includeVariables:
    description: Comma-separated list of variable names to include
    required: false
    default: ''
outputs:
  secretNames:
    description: Comma-separated list of distinct secret names found in AL-Go settings, Alpaca backend and IncludeSecrets
    value: ${{ steps.GetConfigsForSync.outputs.secretNames }}
  variablesJson:
    description: JSON object with variable values found in workflow variables for variable names defined in Alpaca backend and IncludeVariables
    value: ${{ steps.GetConfigsForSync.outputs.variablesJson }}
runs:
  using: composite
  steps:
    - id: GetConfigsForSync
      name: Get COSMO Alpaca Configs For Sync
      shell: ${{ inputs.shell }}
      env:
        _token: ${{ inputs.token }}
        _mode: ${{ inputs.mode }}
        _includeSecrets: ${{ inputs.includeSecrets }}
        _workflowVariablesJson: ${{ inputs.workflowVariablesJson }}
        _includeVariables: ${{ inputs.includeVariables }}
      run: |
        ${{ github.action_path }}/GetConfigsForSync.ps1 -Token $env:_token -Mode $env:_mode -IncludeSecrets $env:_includeSecrets -WorkflowVariablesJson $env:_workflowVariablesJson -IncludeVariables $env:_includeVariables
branding:
  icon: list
  color: blue
